# Voxels

A programming project I did in 2012

## Requirements

- Windows
- MinGW
- Cmake (optional)

## Build

Either `build.bat` or:

- `mkdir build`
- `cd build`
- `cmake .. -G "MinGW Makefiles" && mingw32-make`
